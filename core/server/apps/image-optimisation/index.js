var config     = require('../../config'),
    errors     = require('../../errors'),
    i18n       = require('../../i18n');

module.exports = {
    activate: function activate() {
        console.log("ACTIVATED BEATCH!");
    }
};
